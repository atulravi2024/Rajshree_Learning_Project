# 🔄 SIDEBAR 3D FLIP LOGIC (v2.0)

- **ID**: `06.05`
- **Version**: `v2.0`
- **Focus**: Interactive Perspective Switching
- **Target**: `rajshree_core.js` + `rajshree_core.css`

---

## 🏗️ 3D STACKING ARCHITECTURE

### The Container Layer
- **Class**: `.flipper-container`
- **Property**: `perspective: 1000px`
- **Function**: Establish the 3D space for the inner console to rotate within.

### The Animation Core
- **Class**: `.flipper-inner`
- **Property**: `transition: transform 0.6s cubic-bezier(0.4, 0, 0.2, 1)`
- **Property**: `transform-style: preserve-3d`
- **Trigger**: The class `.flipped` is applied to this element via JavaScript.

### The Faces (Front & Back)
1. **Front (`.flipper-front`)**: Displays the standard Domain Overview (KPIs, Bar/Line Charts).
2. **Back (`.flipper-back`)**: Displays the "Detailed Audit" view (Resource lists, specific fidelity leaks).
   - **Critical CSS**: Must have `-webkit-backface-visibility: hidden` (for Safari/Chrome stability) and `transform: rotateY(180deg)`.

---

## ⚙️ ACTIVATION SEQUENCE

1. **User Interaction**: Click the `card-icon` (Sentinel) located in the `.console-header`.
2. **JS Event**: `toggleSidebarFlip()` is invoked.
3. **State Change**: `flipper.classList.toggle('flipped')`.
4. **Icon Sync**: Since the flip logic uses absolute positioning for faces, the header icon remains common and functional on both sides.

---

## 🤖 AI DIRECTIVES
- **Initialization**: On every `selectDomain()` call, the `.flipped` class MUST be removed to ensure the user starts on the "Front" face.
- **Icon Re-init**: Calling `lucide.createIcons()` after a flip is recommended to ensure any new `<i>` tags on the back face are rendered.

---

## 🛠️ HUMAN VERIFICATION
- [ ] Click the Sentinel icon; verify the flip animation completes in exactly 0.6s.
- [ ] Check for "Ghosting" (text from the front bleeding into the back); resolve via `backface-visibility`.
- [ ] Ensure the "Traffic Chart" on the front face remains responsive after rotating back from the "Audit" face.

---
#Interactive #3D #UX #Fidelity #Animation
