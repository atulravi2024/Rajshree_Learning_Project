# 🗺️ INTERNAL AUDIT VERSION 2.0: GLOBAL NAVIGATION ENGINE

- **ID**: `06.34`
- **Version**: `v2.9`
- **Domain**: 06 — Internal Audit
- **Context**: Cross-Project Navigation parity & Card-Flip Orchestration
- **Source**: `nav_master.js`, `nav_back.js`, `nav_front.js`

---

## 🏛️ NAVIGATION PARITY (`nav_master.js`)

The Frontier Hub ensures 100% navigation parity across the Dashboard, Map, Terminal, and Assets domains.

### 1. Active Link Synchronization
- **Mechanism**: Reads the current `window.location.pathname`.
- **Target**: Iterates through `.nav-link` elements and applies the `.active` class to the matching anchor.
- **Visual**: Triggers the `updateVisualSettings` RGB glow for the newly activated link.

### 2. Domain Redirection
- **Logic**: Handles the standard redirection patterns:
    - `/dashboard/dashboard.html`
    - `/map/map.html`
    - `/terminal/global/global.html`
    - `/assets/assets.html`

---

## 🔄 SIDEBAR CARD-FLIP ENGINE (`nav_back.js`, `nav_front.js`)

A centralized 3D state-machine used across all project domains to reveal detailed audit reports.

### 1. Front-to-Back Transition (`nav_back.js`)
- **Trigger**: Click on `.card-icon` (Dashboard) or `.nav-mode-trigger` (Map).
- **Mechanism**: 
    - Toggles `.flipped` on the `#console-body` or `.nav-side-card-inner`.
    - Forces a delayed `opacity: 1` on the back-face content to prevent layout z-fighting during rotation.

### 2. Parity Rules (`nav_front.js`)
- **Logic**: 
    - Ensures that any "Close" or "Discard" action on a modal or sidebar automatically resets the flip state to the Front face.
    - Clears any temporary `.flipped` classes on the `DOMContentLoaded` event of the next page load.

---
#GlobalNav #CardFlip #StateParity #v2.9
