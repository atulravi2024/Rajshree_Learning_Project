# 🔍 INTERNAL AUDIT VERSION 2.0: SEARCH ENGINE LOGIC

- **ID**: `06.22`
- **Version**: `v2.6`
- **Domain**: 06 — Internal Audit
- **Context**: Fuzzy Search, Aliases & Autocomplete
- **Source**: `map_search_engine.js`

---

## 🗺️ LOCATION SEARCH (`searchDeepLocation`)

The search engine is built for "Anyway from Anyway" reliability, handling common name variations and nested data structures.

### 1. Alias & Synonym Redirection
- **Mechanism**: Checks `MAP_SEARCH_ALIASES` for alternate names before querying the database.
- **Registry (9 Variations)**: 
    - `bombay` -> `mumbai`, `bangalore` -> `bengaluru`, `madras` -> `chennai`.
    - `calcutta` -> `kolkata`, `pondy` -> `puducherry`, `banaras` -> `varanasi`.
    - `trichy` -> `tiruchirappalli`, `baroda` -> `vadodara`, `cochin` -> `kochi`.

### 2. Matching Hierarchy
1.  **Exact Registry Match**: O(1) lookup in `window.LOCATION_COORDS`.
2.  **Recursive Tree Search**: Deep traversal using `findNestedCoords()` if searching via Country/State labels.
3.  **Fuzzy String Inclusion**: Collecting all partial `string.includes(q)` results.
4.  **Fallback Resolution**: Returns the first valid fuzzy record to fulfill "Anyway from Anyway" reliability.

---

## ⌨️ AUTOCOMPLETE ENGINE

A real-time suggestion system designed for sub-ms feedback.

### Technical Parameters
- **Debounce**: 300ms delay to prevent excessive CPU load.
- **Trigger Depth**: Activates after 3 characters are typed.
- **Search Limit**: Constrained to the **Top 5** results to preserve UI performance.
- **Recursive Suggestions**: Iterates through `LOCATION_DATA[Country][State][City]` to build suggestion objects containing `{city, state, country, coords}`.

### UI Interaction
- **Rendering**: Dynamically injects `.suggestion-item` into `#map-search-suggestions`.
- **Sync**: Clicking a suggestion instantly calls `window.rotateGlobeToCoords()` and clears the suggestion list.

---
#SearchEngine #FuzzySearch #Autocomplete #Aliases #v2.6
