# 🧠 INTERNAL AUDIT VERSION 2.0: JS LOGIC FLOW

- **ID**: `06.03`
- **Version**: `v2.3`
- **Focus**: Internal Audit Engine Logic ONLY
- **Target**: `memory/06 Internal Audit/v2.0/dashboard/dashboard.js`

---

## ⚙️ EXECUTION FLOW (Step-by-Step)

### Step 1: Global Visual Sync (`visual_sync.js`)
- **Action**: Load `frontier_config_v2` from `localStorage`.
- **Logic**: `applyGlobalVisuals()` maps config sliders to CSS `--accent-rgb` and `--glow-intensity` variables.
- **Sync**: Listens for the `storage` event to ensure cross-tab parity.

### Step 2: Dashboard State Initialization
- **Action**: Load `DOMAIN_DATA` structure and map to `window.MEMORY_MAP`.
- **KPI Persistence**: `initDataRefresh()` calculates jitter but stores base values in `el.dataset.base` to prevent value drift over cycles.
- **Waveform Animation**: Populate `WAVE_HEIGHTS` and apply randomize `animation-delay` in `buildCardBody()` for organic "jumping" waves.

### Step 3: Neural Console Orchestration
1. **Transition**: `selectDomain(d)` removes `.console-hidden` from the main area.
2. **Title Fitting**: `scaleConsoleTitle()` iteratively shrinks H2 font size (min 0.5rem) using a `while` loop until it fits the `280px` fixed panel width.
3. **Sidebar Rebuild**: `buildSidebarHTML(d)` assembles Overview, AI Interpretation, Terminal and Traffic sections.
4. **Flip Mechanism**: `toggleSidebarFlip()` toggles the `.flipped` class on `#console-body` (perspective 1200px and 0.8s transition).

### Step 4: Sentinel Tooltip Engine
1. **Delegation**: Global `mouseover` listener with 300ms "Premium Feel" delay.
2. **Collision Logic**: `initSentinelTooltips()` calculates `getBoundingClientRect()` and flips the tooltip position if it breaches the bottom viewport boundary.
3. **Boundary Check**: Ensures a `10px` minimum margin from screen edges.

### Step 5: Chart Instance Management
1. **Lifecycle**: `renderSidebarChart(d)` MUST call `activeConsoleChart.destroy()` before creating a new instance.
2. **Accent Sync**: Dynamically pulls `--accent-rgb` from `getComputedStyle(body)` to ensure chart colors match the current theme.

---

## 🤖 AI DIRECTIVES
- **ID Matching**: Ensure `domainId` inside `dashboard.js` perfectly matches the `id` prefixes in `00.00_MEMORY_MAP.js`.
- **Instance Management**: Never initialize a chart without checking if a previous instance exists on that canvas ID.
- **Spark Data**: When generating new spark data, the array length must remain consistent (15 points) for visual stability.

---

## 🛠️ HUMAN VERIFICATION
- [ ] Inspect the Console Log for any `Chart.js` instance leakage warnings.
- [ ] Click through all 9 domains; verify that the sidebar metrics accurately reflect the `DOMAIN_DATA` keywords and resources.
- [ ] Click the Sentinel Icon in the sidebar header; confirm it triggers the 3D flip animation.
- [ ] Confirm that Domain 09 (Audio Script) is auto-selected on initial load.

---
#Logic #Architecture #JavaScript #StepByStep
