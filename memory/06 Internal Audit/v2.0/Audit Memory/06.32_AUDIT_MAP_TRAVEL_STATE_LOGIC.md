# 🛰️ INTERNAL AUDIT VERSION 2.0: MAP TRAVEL & STATE LOGIC

- **ID**: `06.32`
- **Version**: `v2.9`
- **Domain**: 06 — Internal Audit
- **Context**: Viewport State Maintenance & Travel Ops
- **Source**: `map_travel_settings.js`, `map_state.js`, `map_controls.js`

---

## 🧭 TRAVEL OPERATIONS (`map_travel_settings.js`)

The navigation system features a multi-tiered travel engine for varying audit granularities.

### 1. Pointer Mode Management
- **Registry**: 15 Assets (`circle`, `plane`, `jet`, `rocket`, `satellite`, `drone`, `helicopter`, `ship`, `submarine`, `car`, `bus`, `train`, `motorcycle`, `bicycle`, `walking`).
- **Logic**: 
    - Selection updates `window.SELECTED_POINTER_ICON`.
    - Triggers `window.createIconTexture` for high-fidelity sprite re-generation.
    - Updates local speed constant (range: 5 to 28,000 km/h) for duration calculations.

### 2. Physical Constants (Altitude & Velocity)
The system operates in **AUTO (Sync)** and **MANUAL (Override)** modes.

#### Altitude Offsets (`ICON_ALTITUDE_LEVEL`)
- **L1 (Surface)**: 0 units (Car, Walking, Ship).
- **L2 (Low Air)**: 4 units (Helicopter, Drone).
- **L3 (Mid Air)**: 8-12 units (Commercial Plane/Jet).
- **L4 (Orbital)**: 18-22 units (Satellite, Rocket).

#### Speed Multipliers (`ICON_SPEED_MULTIPLIER`)
- **Manual Steps**: 0.25x, 0.5x, 1x, 2x, 4x.
- **Auto-Sync Ceiling**: Up to **8.0x** for Satellite assets.

---

## ⚙️ VIEWPORT STATE MAINTENANCE (`map_state.js`)

The audit map maintains a persistent internal state to prevent visual desynchronization during high-frequency telemetry.

- **Global Flags**:
    - `_mapGlobeDesign`: High-Fidelity vs. Wireframe.
    - `_mapViewMode`: Satellite vs. Atlas.
    - `_mapThemeMode`: Light vs. Dark.
    - `_mapAtmosphere`: Boolean.
    - `_mapSunlight`: Boolean.
- **Sync Logic**: Each state change is mirrored in the local `window.STATE` object to ensure consistent "Deep Scan" refreshes.

---

## 🎮 THREE.JS CONTROL ARCHITECTURE (`map_controls.js`)

Customized environment interaction for the Lead Auditor.

- **OrbitControls Protocol**:
    - `enableDamping`: true (Smoothness factor: 0.05).
    - `minDistance`: 50 (Surface proximity lock).
    - `maxDistance`: 500 (Atmospheric exit lock).
    - `autoRotate`: Controlled via the UI `btn-rotation` toggle.
- **Interaction Constraints**: Zooming and panning sync with the `#globe-zoom-slider` and direct keyboard shortcuts (Ctrl/Alt).

---
#TravelOps #StateManagement #OrbitControls #ThreeJS #v2.9
