# 📋 AUDIT WORKFLOW (v2.0)

- **ID**: `06.04`
- **Version**: `v2.0`
- **Focus**: Internal Audit Hub Workflows ONLY
- **Context**: Project Quality Assurance Standards

---

## 🏗️ THE AUDIT CYCLE (Step-by-Step)

### Phase 1: Structural Integrity Check
1. **Human/AI Action**: Open the [Frontier Hub](file:///c:/Users/Atul%20Verma/.openclaw/workspace/RajShree_Project/Rajshree%20Learning%20Project/memory/06%20Internal%20Audit/v2.0/index.html).
2. **Step**: Verify that all 9 domain slots are populated. Empty slots indicate a JS failure in `DOMAIN_DATA` mapping.
3. **Step**: Match the "Artifact Count" in the sidebar against the `window.MEMORY_MAP` registry. Discrepancies must be logged in the Changelog immediately.

### Phase 2: Visual & Interaction Audit
1. **Interactive Step**: Click on Domain 06 (Internal Audit). Confirm the sidebar title changes to "INTERNAL AUDIT // 06".
2. **Hover Step**: Hover over the "LAUNCH DOMAIN TERMINAL" button. Confirm that the button scales to **1.3x** without clipping.
3. **Graph Step**: Verify that the Sparkline gradients are rendering smoothly without stepping or banding.

### Phase 3: Resource Sync Protocol
1. **Step**: Scroll to the "Resources" list in the sidebar.
2. **Human/AI Action**: Cross-reference each file listed with the actual file system path.
3. **Step**: If a new artifact is added to a domain, it MUST be added to the `resources` array in `rajshree_core.js` to appear in the Hub.

---

## 🤖 AI AGENT INSTRUCTIONS (Step-by-Step)
1. **Initialization**: On starting an audit, the agent must check `06.00_AUDIT_INDEX.md` first to understand the current sub-layer structure.
2. **Verification Loop**: Run a recursive `ls` command on the Current State directory and verify it against Phase 1, Step 3 above.
3. **Reporting**: Any architectural drift found must be reported with a "FIDELITY ALERT" header in the terminal.

---

## 🛠️ HUMAN VERIFICATION
- [ ] Confirm that clicking the "X" on the sidebar closes the console correctly.
- [ ] Verify that the "Initial Loading" glitch animation is visible for < 500ms.
- [ ] Check that the sidebar traffic chart renders the latest 15 points from `d.spark`.

---
#QA #Workflow #Standards #Fidelity #StepByStep
