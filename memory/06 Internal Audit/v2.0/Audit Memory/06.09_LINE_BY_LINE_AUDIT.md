# 🕵️ LINE-BY-LINE TECHNICAL AUDIT (v2.0)

- **ID**: `06.09`
- **Version**: `v2.0`
- **Focus**: exhaustive Code breakdown
- **Audit Date**: 2026-03-22

---

## 📄 INDEX.HTML: THE STRUCTURAL BACKBONE

| Line(s) | Element/Logic | Technical Significance |
|:---|:---|:---|
| 1-10 | `<!DOCTYPE html>`, `<head>` | Standard HTML5 boilerplate with `viewport` optimizations for mobile scaling. |
| 11-12 | Google Fonts | Imports `Outfit` (Headings) and `Fira Code` (Technical mono) for the HUD aesthetic. |
| 13-14 | CSS Imports | Links `rajshree_core.css` (The primary visual engine). |
| 17-26 | `<nav class="glass-nav">` | Floating navigation bar. Uses `glassmorphism` tokens for background blur. |
| 23 | `#notification-bell` | Anchor point for the Notification Hub; contains the `.nav-badge` for unread counts. |
| 29-38 | `.portal-header` | Dynamic header. The `h1` and `span` are targets for JS-driven status updates. |
| 41-48 | `.domain-portal` | The primary workspace for card injection. `#domain-grid` is populated by `initApp()`. |
| 51-68 | `.neural-console` | The Right Sidebar. Note the `:has()` CSS trick used later for overflow management. |
| 59 | `#console-title` | Dynamic target for `scaleConsoleTitle()` font-reduction logic. |
| 62 | `#console-body` | Scroll-locked container for the `.console-flipper`. |
| 71-109 | `#notification-dropdown` | Absolute-positioned HUD for quick alerts. Uses Lucide icons for semantic clarity. |
| 112-127 | `#alerts-modal` | The Central Alerts Modal. High z-index (`3000`) ensures it overlays all other elements. |
| 131 | `rajshree_core.js` | The Neural Engine; script placement at body-end ensures DOM readiness. |

---

## 🎨 RAJSHREE_CORE.CSS: THE VISUAL ENGINE

| Line(s) | Category/Property | Technical Significance |
|:---|:---|:---|
| 1-35 | Root Variables | Defines the "Fidelity Token System" (`--cyan`, `--border-glow`, `--side-w`). |
| 45-80 | Scanline Animation | Uses `linear-gradient` with `background-position` cycling to create the CRT effect. |
| 120-150 | `.glass-nav` | `backdrop-filter: blur(14px)` and `border: 1px solid rgba(255,255,255,0.05)`. |
| 230-260 | `.domain-card` | Core interactive node. Uses `transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.25)` for weighted scale. |
| 280-310 | `.sentinel-aura` | Master pulsing animation for `stable` states. Green glow at `4s` cycle. |
| 885 | `.neural-console` | `transition: transform 0.4s` for smooth hiding; uses `:has()` to show overflow on hover. |
| 1163-1202 | `.btn-launch` | Scales to `1.3x`. The `animation: float-hover-v2` adds 3D floating on hover. |
| 1583-1616 | `.console-flipper` | `perspective: 1200px` and `rotateY(180deg)`. Critical for 3D face switching. |
| 1604 | `backface-visibility` | `hidden` - prevents overlapping artifacts when rotating. |
| 1650-1665 | `.audit-item` | Tactile `translateX(4px)` on hover to indicate high-fidelity interactivity. |
| 1702-1714 | Header Icon States | `rotate(10deg)` on hover for playful feedback. |

---

## ⚙️ RAJSHREE_CORE.JS: THE NEURAL ENGINE

| Line(s) | Function/Logic | Technical Significance |
|:---|:---|:---|
| 50-100 | `initApp()` | Master lifecycle Hook. Initializes Lucide, Charts, and populates the grid with `DOMAIN_DATA`. |
| 150-180 | `buildCardHTML()` | Sanity-checked HTML injection. Sanitizes tooltips and applies dynamic animation delays to `wave-bars`. |
| 210-250 | `selectDomain()` | Multi-step trigger: 1. Updates Global State; 2. Toggles `active` class; 3. Re-renders sidebar. |
| 805-826 | `scaleConsoleTitle()` | **Critical Algorithm**: Decrements `fontSize` by `0.05rem` until text fits `280px` limit. Prevents UI overflow. |
| 840-907 | `buildSidebarHTML()` | The `Fidelity Renderer`. Generates the front/back 3D faces and injects the `logText` based on card status. |
| 1044-1115 | `initNotifications()`| Event delegation for the #notification-bell. Handles toggle states and mark-read cycles. |
| 1134-1167 | `renderModalAlerts()`| Category filtering logic. Re-inits `lucide.createIcons()` to ensure icons appear in dynamic modal content. |
| 1200-1212 | `toggleRead()` | Local State Management. Syncs the `NOTIFICATIONS` array and refreshes all relevant UI nodes. |

---

## 🛠️ AUDIT CONCLUSION
The codebase is **v2.0-Fidelity Ready**. All interaction timings are optimized for "CMDR ATUL VERMA" levels of responsiveness. Any future changes must maintain the `cubic-bezier` weights defined in the CSS.

---
#Audit #Technical #CodeBreakdown #Fidelity #v2.0
