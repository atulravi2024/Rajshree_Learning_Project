# 🧱 INTERNAL AUDIT VERSION 2.0: HTML STRUCTURE

- **ID**: `06.01`
- **Version**: `v2.0`
- **Focus**: Internal Audit Hub Features ONLY
- **Target**: `memory/06 Internal Audit/v2.0/dashboard/dashboard.html`


---

## 📋 META & IDENTITY REGISTRY

| Meta Tag | Content | Purpose |
|:---|:---|:---|
| `ai-role` | `Frontier Protocol Hub` | Designates the system role for AI agents. |
| `project-version` | `v2.0-Fidelity` | Hard-coded version for cache/state validation. |
| `viewport` | `width=device-width, initial-scale=1.0` | Mobile-responsive touch target support. |

---

## 🏗️ DOM ARCHITECTURE (Step-by-Step)


### Step 1: Cinematic Foundation
- **AI/Human Action**: Verify the `<body class="theme-frontier">` is present. This class triggers all global CSS tokens.
- **Layers**:
  1. `<div class="background-grid"></div>`: Provides the 55px neon grid.
  2. `<div class="holographic-glare"></div>`: Adds the top-down ambient blue glow.

### Step 2: Diagnostic Navigation (`.frontier-nav`)
- **Brand Registry**: Ensure the `<div class="nav-brand-icon">` contains the `clipboard-check` Lucide icon.
- **Navigation Center**: Points strictly to Dashboard, Terminal, Map, and Assets.
- **Settings Activation**: `#nav-settings-btn` triggers the `showSettingsMenu()` logic.
- **User Identity**: Pre-set to ATUL VERMA via `nav-user-info` with rank LEAD AUDITOR.

### Step 2.5: System Notification Hub & Settings
- **Elements**: 
  - `#notification-bell`: Houses the `bell` icon and `.nav-badge`.
  - `#notification-dropdown`: Absolute-positioned panel for alert listings.
  - `#alerts-modal`: Central full-screen overlay with `.modal-filter` (36px).
  - `.settings-view`: Injected into `.console-body` for configuration.

### Step 3: Workspace Grid Layout (`.frontier-main`)
- **Left Panel (`.domain-portal`)**: Houses the `.domain-grid` (injected via `rajshree_core.js`).
- **Right Panel (`.neural-console`)**: High-fidelity sidebar. 
  - **Critical Element**: `<div class="console-body" id="console-body">` houses the 3D flip container.
  - **Flipper Structure**:
    - `.console-front`: Standard Overview face.
    - `.console-back`: Detailed Audit face.

### Step 4: Component Injection Nodes
- `#settings-sidebar-container`: The target for dynamic config module injection.
- `#domain-grid`: The flex/grid container for the 12 domain cards.
- `#console-body`: The dynamic content area for the 3D flip interaction.
- `#sentinel-tooltip-float`: Absolute-positioned global singleton for tooltips.

---

## 🤖 AI DIRECTIVES (Step-by-Step)
1. **Script Validation**: Ensure `../../../00 System & Guardrails/00.00_MEMORY_MAP.js` is loaded BEFORE `dashboard.js`.
2. **Icon Re-init**: Calling `lucide.createIcons()` is mandatory after any DOM injection.

3. **Responsive Check**: Verify that `grid-template-columns` for the main layout shifts from `1fr 360px` to a stacked view on screens < 1200px.

---

## 🛠️ HUMAN VERIFICATION
- [x] Verify that clicking a domain card updates the `#console-title`.
- [ ] Confirm that the notification badge (`.nav-badge`) is visible in the top right.
- [ ] Verify that clicking the bell icon toggles the `.notification-dropdown`.
- [ ] Confirm that the `.glitch-icon` is visible in the initial "WELCOME" state.

---
#Audit #HTML #StepByStep #Fidelity
