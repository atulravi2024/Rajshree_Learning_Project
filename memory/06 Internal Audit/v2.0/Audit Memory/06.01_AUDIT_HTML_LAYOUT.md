# 🧱 INTERNAL AUDIT VERSION 2.0: HTML STRUCTURE

- **ID**: `06.01`
- **Version**: `v2.0`
- **Focus**: Internal Audit Hub Features ONLY
- **Target**: `memory/06 Internal Audit/v2.0/dashboard/dashboard.html`


---

## 📋 META & IDENTITY REGISTRY

| Meta Tag | Content | Purpose |
|:---|:---|:---|
| `ai-role` | `Frontier Protocol Hub` | Designates the system role for AI agents. |
| `project-version` | `v2.0-Fidelity` | Hard-coded version for cache/state validation. |
| `viewport` | `width=device-width, initial-scale=1.0` | Mobile-responsive touch target support. |

---

## 🏗️ DOM ARCHITECTURE (Step-by-Step)


### Step 1: Cinematic Layering (Source: `background.js`)
- **Background Container**: `<div class="background-container">` (Injected dynamically).
- **Core Layers**:
  1. `<div class="background-grid">`: The 55px structural neon grid.
  2. `<div class="holographic-glare">`: Atmospheric top-down lighting.
  3. `<div class="scanline-overlay">`: Periodic 4px horizontal CRT lines.
  4. `<div class="crashing-noise">`: Micro-grain texture for "analog" fidelity.

### Step 2: Global Navigation (`.frontier-nav`)
- **Brand Identity**: 
  - `<div class="nav-brand-icon">`: Houses `clipboard-check` (Lucide).
  - `brand-text`: Contains `<h1>RAJSHREE</h1>` and `<span>INTERNAL AUDIT</span>`.
- **Center Links**: `DASHBOARD`, `TERMINAL`, `MAP`, `ASSETS`. Active state managed via `.nav-link.active`.
- **User Unit**:
  - `nav-avatar`: Micah SVG via DiceBear (Seed: Atul).
  - `nav-user-info`: Displays `LEAD AUDITOR` (Rank) and `ATUL VERMA` (Name).
- **Control Suite**: 
  - `#notification-bell`: Contains `bell` icon and unread `.nav-badge`.
  - `#nav-settings-btn`: Triggers the settings overlay.
  - `.logout`: Exit protocol.

### Step 2.5: System Notification Hub & Settings
- **Notification Engine**: Injects `.notification-dropdown` and `#alerts-modal` (36px filter blur).
- **Settings Hook**: `#settings-sidebar-container` for dynamic injection of the configuration panel.
- **Visual Sync**: `visual_sync.js` applies body classes (e.g., `.theme-frontier` or `.chroma-distort`).

### Step 3: Neural Workspace (`.frontier-main`)
- **Initial Layout**: `.frontier-main.console-hidden` (Right panel hidden until selection).
- **Domain Portal**: Houses the `#domain-grid` flex container for card injection.
- **Neural Console (Sidebar)**:
  - **Dynamic Title**: `#console-title` (e.g., "00 SYSTEM & GUARDRAILS").
  - **Body Node**: `#console-body` houses the flipping container (`.console-front` / `.console-back`).
  - **Welcome State**: Initial `.console-welcome` with icon (⚓).

### Step 4: Component Injection Nodes
- `#settings-sidebar-container`: The target for dynamic config module injection.
- `#domain-grid`: The flex/grid container for the 12 domain cards.
- `#console-body`: The dynamic content area for the 3D flip interaction.
- `#sentinel-tooltip-float`: Absolute-positioned global singleton for tooltips.

---

## 🤖 AI DIRECTIVES (Line-by-Line)
1. **Script Sequencing**: `MEMORY_MAP.js` -> `visual_sync.js` -> `background.js` -> `settings.js` -> core scripts.
2. **Icon Integrity**: Calling `lucide.createIcons()` is mandatory after any DOM injection in ASSETS, DASHBOARD, or MAP.
3. **Tooltip Singleton**: Ensure `#sentinel-tooltip-float` is created precisely once if absent.

---

## 🛠️ HUMAN VERIFICATION
- [x] Verify that clicking a domain card updates the `#console-title`.
- [x] Confirm that the glitch icon (⚓) is visible in the initial "WELCOME" state.
- [ ] Confirm that the notification badge (`.nav-badge`) is visible in the top right.
- [ ] Verify that clicking the bell icon toggles the `.notification-dropdown`.
- [ ] Inspect `.background-container` in DevTools to confirm all 4 layers are active.

---
#Audit #HTML #StepByStep #Fidelity
