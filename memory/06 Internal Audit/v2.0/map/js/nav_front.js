/**
 * nav_front.js - Logic for Front Side (Project Analysis, Reports, Critical info).
 * NOTE: Most common UI listeners are now centralized in map_ui.js.
 */

function initNavFront() {
    // Front-specific logic can go here. 
    // Currently, all main bottom bar buttons are handled in map_ui.js to avoid double-firing.
    
    // Future expansion for Analysis-specific tabs or interactions
    console.log("Nav Front initialized (Common UI listeners handled by map_ui.js)");
}

// Ensure global functions are available
window.initNavFront = initNavFront;

