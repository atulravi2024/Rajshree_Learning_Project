# 📜 RAJSHREE LEARNING — CHANGELOG (v15.2)

> *Full version history and migration records. Load during audits and reviews.*

---

## Version History

| Date | Version | Change |
|:---|:---|:---|
| 2026-02-23 | v12.0 | High-Precision Multi-Audit layout rules finalized. Flash card anchoring, dynamic scaling table, fit-screen column logic, slideshow protocol with lockdown, and 3D card flip system established. |
| 2026-02-24 | v13.0 | **CSS Modular Split** — Monolithic `style.css` (19KB) → Unified Modular Vault (8 files across 5 folders: `base/`, `layout/`, `features/`, `themes/`, `lang/`). Created `main.css` master loader with `@import`. Original file renamed to `style_backup_full.bak`. Updated `index.html` to link to `css/main.css`. |
| 2026-02-24 | v13.1 | **Full Audit** — Added complete Assets/Audio section. Documented CSS import order, all animation names, overlay navigation flow (Step 1→5), audio naming conventions. Added `samay_rang/` empty folder note. Fixed Z-Index map (added Overlay: 3000). |
| 2026-02-24 | v13.2 | **Re-Audit & Corrections** — Fixed JS loading order (added missing `utils/helpers.js` between Core and UI). Moved `blink-ring` animation from `animations.css` listing to `overlay.css` (where it actually lives). Corrected system audio file names. |
| 2026-02-24 | v13.3 | **Deep CSS Trigger Audit** — Added complete CSS Trigger & Interaction Map. Documented all hover/active/click/class triggers, pseudo-elements, `:has()` selectors, card flip mechanism, dynamic JS-set variables. Flagged 2 duplicate `@keyframes` (happy-wiggle, title-zoom). |
| 2026-02-24 | v14.0 | **Hybrid Memory Split** — Single 26KB `PROJECT_MEMORY.md` split into 5 files using Frequency × Layer hybrid method. Core memory + 3 on-demand layer files + Archive changelog. Cross-reference system added. |
| 2026-03-04 | v14.2 | **Deep Codebase Memory Sync** — Created `PROJECT_HTML.md`. Updated JS data tree, CSS line counts, category map with new sub-categories. |
| 2026-03-13 | — | **Khali Icon Implementation** — Implemented `red-circle` type in `display.js` for Vyanjan characters ङ, ञ, ण. Added `khali_icon.png` to `assets/images/icons/`. Updated `vyanjan.js` data with `type: 'red-circle'`. |
| 2026-03-14 | v15.0 | **Full Memory Sync & Cleanup** — Deleted temp files (`__pycache__/`, `.pyre_configuration`, `audit_report.md`, `refined_audit_report.md`, `count_new_audio.py`, `fix_audio.py`, `test.mp3`). Updated all memory files with accurate data: corrected audio count from 369→547, data files from 41→37, CSS line counts updated, navbar icons corrected (🌎 not 🌍, 🕐 not ☀️). Created new `01.08_PROJECT_ICONS.md` for comprehensive icon tracking. Updated navigation icon maps to match `router.js`. |
| 2026-03-14 | v15.1 | **Deep Mapping Audit (Icons & Audio)** — Created `01.09_PROJECT_AUDIO.md`. Conducted deep line-by-line audit of all 547 audio files (matched exactly to file system). Updated `01.08_PROJECT_ICONS.md` with missing title bar emojis, welcome text icons, back button icons, and all 100 number entries. Verified 100% completeness for both Icon and Audio memory files. |
| 2026-03-15 | v15.2 | **Audio Script Integration** — Moved 24 parts of Hindi audio generation scripts to `memory/09 Audio Script/`. Updated all memory files (`ASSETS`, `AUDIO`, `MEMORY`) to include references to these scripts as primary source material for future audio generation. Cleaned up unnecessary files from project root. |

---

## Architecture Decisions

### Why CSS Was Split (v13.0)
- Original `style.css` was 19KB monolith
- Adding more languages, age groups, and features would make it unmanageable
- Modular approach allows swapping themes without touching core code
- Each file can be loaded conditionally via JS in the future

### Why Memory Was Split (v14.0)
- Single `PROJECT_MEMORY.md` grew to 26KB after deep CSS audit
- Most sessions only need core rules (~6KB), not the full trigger map
- Frequency × Layer hybrid ensures minimal token usage per session
- Cross-references in core file guide to load details on-demand

### Backup Strategy
- All original monolith files preserved with `.bak` extension
- `.bak` files are never loaded by browser or build tools
- Serve as rollback safety net if modular architecture has issues

---
*Last Updated: 2026-03-15 | v15.2 — Added 09 Audio Script integration records*
